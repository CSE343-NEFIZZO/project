<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

        <group android:checkableBehavior="single">
            <item android:id="@+id/nav_profile"
                android:icon="@drawable/ic_account"
                android:title="Profile"/>
            <item android:id="@+id/nav_forum"
                android:icon="@drawable/ic_forum"
                android:title="Forum"/>
            <item android:id="@+id/nav_addFood"
                android:icon="@drawable/ic_add_recipe"
                android:title="Add Recipe"/>
            <item android:id="@+id/nav_list"
                android:icon="@drawable/ic_list_recipe"
                android:title="List Recipe"/>
            <item android:id="@+id/nav_search"
                android:icon="@drawable/ic_search"
                android:title="Search"/>

    </group>
</menu>